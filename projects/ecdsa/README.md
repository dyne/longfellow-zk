# Longfellow ZK ECDSA

This standalone, header-only circuit project consumes an installed
`LongfellowZK` package and exports `LongfellowZKECDSA::ecdsa` for applications
such as mdoc.  Its public include hierarchy remains `circuits/ecdsa/...`.
