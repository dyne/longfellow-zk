# Longfellow ZK BIP340

This standalone project provides the BIP340 circuit headers and vectors.  It
consumes an installed `LongfellowZK` package and exports
`LongfellowZKBIP340::bip340` for downstream circuit projects.
