# Longfellow ZK mdoc

This standalone application project owns the mdoc circuits, C API, CLI, WASM
adapter, test fixtures, and zstd decompression support.  It consumes installed
`LongfellowZK` and `LongfellowZKECDSA` packages; zstd is a private dependency.
