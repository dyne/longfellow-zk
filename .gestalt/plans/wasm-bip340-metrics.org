#+TITLE: WASM BIP340 Metrics
#+SUBTITLE: Exercise BIP340 in WASM and log elapsed time plus compressed byte sizes
#+DATE: 2026-07-05
#+KEYWORDS: wasm bip340 metrics ci tobuf

* DONE [#A] Add WASM BIP340 smoke execution
:PROPERTIES:
:Effort: M
:Goal: Ensure CI exercises the imported BIP340 circuit through the WASM module, not only through native tests.
:Notes: Keep BIP340 semantic vectors in upstream longfellow-zk; dyne only needs import/build/runtime coverage.
:END:

** DONE [#A] Export BIP340 smoke through tobuf
:PROPERTIES:
:Why: The current WASM exports cover mdoc only, so BIP340 can compile natively while never being instantiated in WASM.
:Change: Add a minimal BIP340 circuit compile smoke export, wire it into the WASM link exports, and expose it from the JS bindings.
:Tests: Run make wasm and node test/wasm_test.mjs.
:Done when: The WASM test suite can call the BIP340 export and fail loudly if the circuit cannot be built in WASM.
:END:

** DONE [#A] Serialize BIP340 circuit artifact
:PROPERTIES:
:Why: Building the BIP340 circuit in memory does not exercise the artifact path used by other WASM circuit flows.
:Change: Serialize the BIP340 circuit with the secp256k1 field id, read it back once, and return the serialized bytes as hex from the WASM smoke export.
:Tests: Run make wasm and node test/wasm_test.mjs.
:Done when: The BIP340 WASM test logs a real compressed circuit byte size from circuit_data_hex.
:END:

* DONE [#B] Log WASM step metrics
:PROPERTIES:
:Effort: S
:Goal: Make CI logs show timing and compressed byte sizes for each WASM circuit/proof step.
:Notes: Hex length is transport encoding size; report decoded byte length for circuit and proof payloads.
:END:

** DONE [#B] Add timing and compressed-size logs
:PROPERTIES:
:Why: Existing WASM logs print hex character counts and omit per-step elapsed time.
:Change: Wrap each WASM execution step with millisecond timing and log compressed payload byte sizes for generated or consumed circuits/proofs.
:Tests: Run node test/wasm_test.mjs against the built WASM module.
:Done when: CI logs include ms timings and compressed byte sizes for BIP340, circuit generation, proof generation, and proof verification paths.
:END:

* DONE [#A] Split WASM test suites
:PROPERTIES:
:Effort: S
:Goal: Allow fast BIP340-only WASM testing without running the long mdoc proof path.
:Notes: Keep mdoc available for explicit runs, but use BIP340 for this PR's focused WASM test.
:END:

** DONE [#A] Add suite selection
:PROPERTIES:
:Why: The current WASM runner always executes both BIP340 and mdoc tests, making focused BIP340 validation slow.
:Change: Add a suite selector for bip340, mdoc, and all; keep CI running all suites while allowing local focused BIP340 runs.
:Tests: Run node test/wasm_test.mjs --suite bip340 ./longfellow-zk.wasm.
:Done when: BIP340-only execution skips mdoc work and still logs timing plus compressed circuit size.
:END:

* DONE [#A] Complete BIP340 WASM proof flow
:PROPERTIES:
:Effort: M
:Goal: Make the BIP340 WASM suite exercise circuit serialization, proof generation, proof serialization, proof parsing, and verification.
:Notes: Use Bitcoin BIP-340 vector 0 in WASM so the platform test exercises the imported real-vector path end to end.
:END:

** DONE [#A] Prove and verify BIP340 in WASM
:PROPERTIES:
:Why: The BIP340 WASM path currently serializes only the circuit artifact and does not prove or verify.
:Change: Generate a witness from Bitcoin vector 0, prove it, serialize and parse the proof, verify it, and return proof size metrics.
:Tests: Run make wasm and node test/wasm_test.mjs --suite bip340 ./longfellow-zk.wasm.
:Done when: BIP340-only WASM logs circuit and proof compressed byte sizes and passes verification.
:END:

* DONE [#A] Correct CI and BIP340 phase logs
:PROPERTIES:
:Effort: S
:Goal: Keep CI coverage broad while making BIP340 proof and verification phases visible in WASM logs.
:Notes: The suite selector is for local focus; CI must exercise every WASM suite.
:END:

** DONE [#A] Run all WASM suites in CI
:PROPERTIES:
:Why: CI was narrowed to BIP340 during local debugging, dropping mdoc WASM coverage.
:Change: Restore the CI command to run the default all-suite WASM test path.
:Tests: Inspect workflow and run the focused BIP340 suite locally.
:Done when: The workflow no longer passes --suite bip340 in CI.
:END:

** DONE [#A] Print BIP340 proof and verify phase metrics
:PROPERTIES:
:Why: The BIP340 smoke output showed one aggregate timing line, hiding proof generation and verification work.
:Change: Return per-phase BIP340 timings from WASM and print separate log lines for circuit, witness, proof, serialization, and verification phases.
:Tests: Run node test/wasm_test.mjs --suite bip340 ./longfellow-zk.wasm.
:Done when: BIP340 WASM logs include explicit proof generation and proof verification lines with ms and compressed byte sizes.
:END:
