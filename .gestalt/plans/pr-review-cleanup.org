#+TITLE: PR Review Cleanup
#+SUBTITLE: Tighten WASM buffer API and trim review findings
#+DATE: 2026-07-05
#+KEYWORDS: wasm buffers review cleanup longfellow-zk

* WIP [#A] Harden WASM buffer boundaries
:PROPERTIES:
:Effort: 1h
:Goal: Ensure WASM buffer APIs never return malformed or truncated JSON as success.
:Notes: The compatibility wrapper should be useful for Wasmtime smoke tests, while _tobuf callers should receive explicit buffer-size errors.
:END:

** DONE [#A] Fail on undersized output buffers
:PROPERTIES:
:Why: `safe_copy` currently truncates successful JSON silently, which can produce malformed output with return code 0.
:Change: Make success paths check output length and return an error when `out_buf` is too small.
:Tests: Run the Node WASM suite and Wasmtime `wasm_generate_circuit` smoke test.
:Done when: Successful calls either return complete JSON or fail with a clear buffer-size error.
:END:

** DONE [#A] Make compatibility circuit output complete
:PROPERTIES:
:Why: `wasm_generate_circuit` currently uses a 4 KB stack buffer and returns truncated JSON to Wasmtime.
:Change: Allocate a heap buffer sized for circuit JSON or fail loudly if it is still too small.
:Tests: Run Wasmtime invoke and parse stdout as JSON.
:Done when: Wasmtime `wasm_generate_circuit` returns parseable JSON or nonzero status with an error.
:END:

* WIP [#B] Tighten JS and attribute handling
:PROPERTIES:
:Effort: 1h
:Goal: Remove leaks and reject malformed attribute payloads before crossing into domain code.
:Notes: Keep changes minimal and dependency-free.
:END:

** DONE [#A] Free JS string arguments
:PROPERTIES:
:Why: JS bindings allocate string arguments in WASM memory and currently only free output buffers.
:Change: Track allocated argument pointers and free them in `finally`.
:Tests: Run the Node WASM suite.
:Done when: String argument allocations are released after each call.
:END:

** DONE [#B] Validate attrs_json shape and hex
:PROPERTIES:
:Why: Attribute JSON parsing accepts malformed structures and invalid CBOR hex, relying on downstream behavior.
:Change: Require an array of objects with string fields, valid hex `cbor_value`, and lengths that fit `RequestedAttribute`.
:Tests: Run the Node WASM suite; add a malformed attrs assertion if cheap.
:Done when: Bad attribute input fails with a clear error buffer message.
:END:

* TODO [#C] Trim small review nits
:PROPERTIES:
:Effort: 20m
:Goal: Remove low-risk noise from the PR before merge.
:Notes: Do not touch workflow action pins; those are user-owned local edits.
:END:

** TODO [#C] Simplify makefile and duplicate checks
:PROPERTIES:
:Why: Duplicate `CXX` assignment, typo text, and redundant nested condition add avoidable clutter.
:Change: Remove the duplicate assignment, fix the typo, and simplify duplicate conditionals.
:Tests: Run the WASM build/test path.
:Done when: Generated behavior is unchanged and CI-equivalent WASM tests pass.
:END:

** TODO [#C] Drop completed execution plan artifact
:PROPERTIES:
:Why: The previous completed plan is useful locally but not part of the project deliverable.
:Change: Remove `.gestalt/plans/wasm-buffer-tests.org` from the PR.
:Tests: No runtime tests needed for this file-only cleanup.
:Done when: Only the active cleanup plan remains until final completion.
:END:
