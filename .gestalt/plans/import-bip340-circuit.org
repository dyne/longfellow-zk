#+TITLE: Import BIP340 Circuit
#+SUBTITLE: Bring upstream secp256k1 BIP-340 verification into longfellow-zk-dyne
#+DATE: 2026-07-05
#+KEYWORDS: bip340 secp256k1 circuit integration vectors wasm ci

* WIP [#A] Import and build BIP340 sources
:PROPERTIES:
:Effort: 2h
:Goal: Add the upstream BIP340 circuit and secp256k1 support files to longfellow-zk-dyne and make native/WASM builds compile them when needed.
:Notes: Keep the import narrow: no new dependencies beyond the existing OpenSSL usage already required by the upstream witness header.
:END:

** WIP [#A] Copy required upstream files
:PROPERTIES:
:Why: The BIP340 circuit depends on the new circuit headers plus secp256k1 field and curve definitions that are absent from dyne.
:Change: Import circuits/bip340 headers, algebra/fp_p256k1.h, ec/p256k1.{h,cc}, and test vector include data.
:Tests: Run a focused compile of a tiny include smoke test or the new BIP340 test binary once added.
:Done when: All imported headers resolve locally without reaching outside src/ or test/.
:END:

** TODO [#A] Wire GNUmake and importer
:PROPERTIES:
:Why: CI re-imports selected upstream files before building, so both sources.mk and scripts/import_upstream.sh must know about secp256k1 support.
:Change: Add ec/p256k1.cc to the library source list, add BIP340/secp256k1 headers to the import allowlist, and ensure POSIX/macOS test builds link OpenSSL crypto for BIP340 tests.
:Tests: Run make clean && make -j$(nproc), then a WASM build to ensure the imported files do not break either target.
:Done when: Native and WASM builds complete with BIP340 files present.
:END:

* TODO [#A] Add BIP340 integration and vector coverage
:PROPERTIES:
:Effort: 3h
:Goal: Cover the BIP340 witness and circuit behavior using the Bitcoin vectors and at least one prover/verifier integration path.
:Notes: Avoid gtest; use a small standalone C++ test binary invoked from Bats so existing CI runs it.
:END:

** TODO [#A] Add standalone BIP340 test binary
:PROPERTIES:
:Why: longfellow-zk-dyne currently has Bats CLI tests, not a native C++ unit-test framework.
:Change: Add test/bip340_test.cc with minimal assertion helpers, vector parsing, valid/invalid evaluation tests, semantic golden checks, circuit size checks, and one ZK prover/verifier round trip.
:Tests: Build and run the binary locally.
:Done when: The binary exits nonzero on failures and reports the covered BIP340 cases on success.
:END:

** TODO [#A] Integrate BIP340 tests into CI suite
:PROPERTIES:
:Why: The new coverage must run in the existing CI paths without adding new framework dependencies.
:Change: Add a Bats case that runs the BIP340 native test binary when present, and keep WASM tests compiling the imported code.
:Tests: Run test/bats/bin/bats test/mdoc.bats, node test/wasm_test.mjs, and the final CI-equivalent native/WASM commands.
:Done when: All local test suites pass and the branch is ready to push.
:END:
